# 内部模型三 Key 轮换与中断恢复核查（2026-09-10）

## 实际核查

- 源码目录与正式运行目录均配置三把不同的全局 Key；另有独立免费模型专用 Key，不混入全局池。
- 09:44–09:46 香港时间，三把全局 Key 的模型列表均成功返回；每把另连续两次完成 DeepSeek-V4-Pro 文本响应，HTTP 200、finish_reason=stop、正文为 OK。只能证明核查时可用，不能据此推断全天剩余额度。
- 旧战略新闻日志把额度、限流、鉴权、模型权限失败合并为一种提示，未保留足够信息回溯每一次的真实类别，因此不能凭旧提示断言三把 Key 全部预算耗尽。
- 旧公共轮换仅把冷却 Key 排到末尾，仍会请求它们，且漏记最后一把失败；冷却只在进程内保存。战略新闻还有独立循环，每次重新从首把开始，放大重复失败日志。

## 修复行为

- Web、研究、运维、战略新闻及模型客户端共享原子落盘的冷却状态，使用进程锁，记录指纹、失败类别、HTTP 状态和重试时间，不记录明文 Key。
- 三把候选逐一故障切换；最后一把同样记录失败。冷却期间跳过已失败的路由；全部冷却则返回明确可重试错误。到期后的新请求重新验证可用性。
- 区分额度、请求限流、模型权限与鉴权失败。限流尊重 Retry-After，缺省 60 秒；额度及权限缺省 600 秒。模型权限按模型隔离，避免影响同一 Key 的其他有权模型。
- 短时连接故障、响应读取中断及可重试服务错误有限退避重试；不将网络中断当作 Key 额度耗尽。公共 urllib 调用最多额外重试两次，遵守执行时间预算；每次重试和轮换都计入现有全局请求队列。
- 非流式推理完整读入后才交给业务层，避免半截数据进入解析及写入。流式内容一旦输出便不从头重放或拼接其他 Key 的响应；已有会话及检查点负责后续恢复。
- 语音合成、语音对齐补接公共轮换，音频或识别响应读完后再进入后续处理。
- 修复重试验证脚本中重构后失效的 verification 文件路径。

## 验证

- 17 组相关测试：283 passed，23 subtests passed。
- 覆盖第三把成功、三把全失败、冷却阻止重复请求、冷却到期恢复、跨进程共享与并发写入、按模型隔离权限、HTTP 403 可见性、同步/异步请求及流式输出前后断线。
- 真实子进程退出后锁释放及检查点续跑通过；额度失败不会成为成功检查点，已完成决策复用而不重复执行。
- 既有任务恢复、新闻写入检查点、报告、语音、清洗与新闻初筛回归通过。18 类输出校验模拟通过，但该模拟本身不等同于 18 条真实端到端业务任务。
- 未通过验收发送任何飞书测试消息或启动正式爬虫。安全发布队列于 10:04:01 香港时间确认 activated、queue is empty；五个运行文件与源码哈希一致，8765 健康接口正常并确认三把全局 Key。远端同步以本轮工具回执为准。

## 下一轮运行前补查

- 发现运维监控守护进程仍从 9 月 3 日运行，文件更新后，其内存中的模型客户端不会自动更新。安全发布工作器补入已加载监控服务的重载，复用原有双次空闲检查；未启用的监控服务保持未启用。
- 监控服务重载失败会保留发布请求和发布包，不报告激活成功。新增隔离脚本测试实际执行成功、未启用和重载失败三条路径，验证空闲检查先于复制、复制先于重载、失败不清空队列。
- 本次补查相关 5 组测试共 49 passed、5 subtests passed，包含轮换、任务恢复、已写入检查点和真实进程中断恢复。
- 安全加载请求 `20260910T102029-53552-6808` 于 10:21:09 香港时间完成并清空队列。Web、调度器和监控进程启动时间均晚于本次发布；原监控 PID 1685 已替换为 54357，五个关键运行文件与源码一致，8765 健康检查通过。

真实预算都耗尽时，代码无法补充额度；任务须保留待处理状态，等待网关额度恢复后重试，不能把失败伪装成成功。
