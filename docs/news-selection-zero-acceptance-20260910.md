# 2026-09-10 午后新闻初筛零入选复核与恢复

## 原因与修复

午后原批次 `2026-09-10@14:00` 新增 41 条，爬虫父任务 `20260910_140056_191951` 已完成候选写入与群通知。初筛任务 `20260910_143636_360211` 两次判断后均因“双周报 41 条整批全部不接受”触发分布门禁。门禁只看数量，不能区分合法零入选与模型偏斜；持久化暂停之后，调度器仍每十分钟创建同输入失败任务。

新增零入选复核协议 1：对达到异常检测规模且零入选的字段，独立重读全部候选与人工基准，不展示原机器理由或学习摘要。每个待审字段均需独立理由和当前标题／摘要中的逐字事实。允许有依据的零入选；如纠正漏选，新增接受仍须通过接受复核协议 3 的事实、业务价值、完整事件分组及去重校验。非本次复核字段与既有人工字段受保护。

零入选复核使用独立持久化键，校验不完整或引文不存在时不保存成功检查点。原机器响应不能自行声明复核通过；人工样本来源版本保持 v6，旧有效分批结果可复用，旧数量门禁暂停不会阻断新协议。接受率异常偏高的原门禁继续生效。

终止性质量阻断使用明确异常类型，扫描重试归档为 `needs_review`，保留失败原因并清空下一重试时间；通用补审队列不会再接手同一批次反复创建失败任务。显式人工重试必须指定原批次，可绕过调度冷却，但不能绕过内容质量校验、人工保护或逐格回读。

## 验证与加载

- 第一组：206 项测试、30 项子测试通过，覆盖选择器、接受复核、零入选与调度恢复。
- 最后改动后：246 项测试、39 项子测试通过，包含飞书读写、人工归属与发布门禁。
- 代码提交 `364853ac7`；安全请求 `20260910T152905-97820-21818` 于 15:29:43 激活、队列清空，两个改动运行模块 SHA-256 与源码一致。
- 公私开发分支已同步；首份私有完整快照 `39d8a1bf4faa9246c535d199ad16701d60127f4d`，备份分支 `backup/main-before-sync-20260910-152930`。
- 授权重试任务 `20260910_153038_307048` 于 15:30:38 启动，原 41 条有效分批检查点全部复用，只补充独立零入选复核。运行结果与最终回读见后续验收记录。

## 证据

正式运行目录 `var/backups/news-selection-zero-20260910/` 保存事前选择器状态、原批次归档、调度状态、机器审计及飞书快照。原始候选、逐条理由和工作区验收证据仅由标准同步脚本进入私有完整快照。


## 正式恢复验收

- 原批次重试于 15:33:47 完成，耗时 3 分 9 秒，实际补充模型请求 2 次。第一步复核全部 41 条，第二步对 5 条拟接受候选做独立事实及事件复核，最终滚动栏接受 1 条、周报接受 1 条，其余各 40 条不接受；没有按配额放宽接受。
- 飞书最终独立回读版本 57651：41 条、82 格与 41 条机器审计逐格一致，差异 0；本批以外既有新闻的两项审核状态变化 0。41 条均有独立零入选复核证据。
- 两字段最终共同接受《苹果发布iPhone Handoff，T-Mobile与德国电信首批支持》。正式发布池包含该 ID，归属本次任务与实际模型；发布池共 70 条。
- 原批次已从待选队列核销，父爬虫归档已关联本次成功任务；原群通知状态、消息 ID 与时间均未改变。
- 正式登录 Chrome 页面已实看“新闻自动初筛7／已完成”、3 分 9 秒、处理 41 条、验证 82 格，完成后活动灯关闭；原失败记录保留可追溯。
- 独立验收脚本按真实 schema 显式映射：飞书行 APP 为 `status`，审计为 `app_status`，发布池 ID 为 `id`，表版本为 `spreadsheetRevision`。早期辅助验收脚本的字段误配输出保留为 `verification-parser-v0/v1.json`，最终以 `verification.json` 的实时回读及有效状态校验为准；未因此重跑模型或写入。

工作区 `var/backups/news-selection-zero-20260910/` 已保存运行证据、事前事后表快照、最终逐格核验、源与运行 SHA-256、回归结果及同步回执；只进入私有完整快照。
