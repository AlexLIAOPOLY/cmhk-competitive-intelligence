# 2026-09-12 个人战略新闻投递恢复

今早03:00批次于07:09完成并入队，09:00时14位订阅者均未发送。阻塞发生在下游图文准备：正文提取把推荐文章当原文、模型判断与布尔字段矛盾、格式正确但业务失败的模型输出被底层缓存，以及单条故障中断整份卡片。用户授权修复全链路、重跑并发完本轮所有订阅者，适当放松门禁，优先V4 Pro。

## 修复

- 识别 `.prose`、香港商报 `.newsDetail`、中通社 `.xlCon` 等正文容器，并用 trafilatura 精确模式补充未知网页布局，正文优先于推荐卡片；结构化新闻和通用article按原网页标题排除无关事件。原媒体标题、description和原始摘录可作证据；不因付费正文截断就否定已有公开事实。AI摘要本身不能作为审核依据。
- 简介独立审核使用五个正反少样本、证据编号和单一 `verdict`，避免布尔与理由冲突。拒绝后真正重新请求，防止命中同一失败模型缓存；只有有具体增量、证据存在的结果才能通过。
- 编辑、审核、事件去重、补图关键词与图片主体文字复核优先 `DeepSeek-V4-Pro`。实际看图继续使用支持图片输入的视觉模型；沿用现有共享Key轮换和限流。
- 同一新闻跨订阅者加锁共用已审核结果。正文与原始摘录纳入版本缓存；单条连续失败临时隔离十五分钟，来源变更可立即重新准备。
- 单条失败记录阶段和原因，继续准备其他条目；在原批次和兴趣范围内换稿并重新去重。不足时发送较少的合格图文，禁止编造、重复凑数或用空图补数。全部失败时保持原队列，不能发空卡冒充完成。只有实际发送的新闻进入个人历史。
- 单条图文180秒、单卡600秒总预算贯穿模型调用与共享等待；共享素材/编辑锁最多等5秒，单次去重最多90秒。投递专用去重遇到错误跳过该组候选，不递归拆成无界补判；其他研究调用保持原严格恢复行为。
- 仅本次实质质量规则未变的技能哈希之间映射有效成功缓存。不同新闻/原始事实/图片及未知技能版本不能复用，拒绝结论不被视为通过。
- 新闻入队立即准备，发送仍按个人时间。失败退避从十五秒逐步增加至五分钟并加随机间隔；失败次数、错误和下次重试可持久化，重启不清除退避。新任务不会被反复失败的旧任务长期抢占。

## 依据

- [Anthropic 提示词最佳实践](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)：使用相关、多样、结构清晰的少样本，并用实际模型评估。
- [OpenAI Structured Outputs](https://openai.com/index/introducing-structured-outputs-in-the-api/)：结构符合schema不代表语义正确；可提供示例或拆成更简单的任务。
- [AWS 重试与退避](https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/)：有界重试、退避和随机间隔降低故障时的重复负载；发送沿用幂等请求。

- 用户追加要求已写入正式skill及editorial引用：识别错误换新闻，不死磕；清理重复条款和旧的单条无限等待。
- 订阅准备线程从3增为8，AI总并发与速率限制不变；发送仍是独立时钟。

## 验证进度

- 最新204项回归、43个子测试通过；新增预算传播、共享锁超时、错误去重不递归、精确技能哈希兼容、来源变更失效。调大订阅并发后18项时钟/预算专项复测通过（与前组重叠）。
- 中兴真实失败样本已用V4 Pro完成独立审核，57.2秒，保存了增量与逐字证据。
- Macau Business原网页实抓读出1101字正文，包含制裁呼吁，未混入澳门美食推荐。
- 本轮共有43条不同候选，正按逐条审核方式准备；已审核结果跨接收者共用，失败项换稿。生产证据目录：`/Users/liaowang/cmhk_public_crawl_app/var/subscriptions/recovery-20260912/`。
- 09:34:24 安全加载完成、queue empty，常驻PID94942确认新skill哈希 `f05fc1032d72d1eb676f2e11004133782e364e1631cb2795491cb5e8984ada41` 及V4 Pro优先；8订阅准备线程的后续小版本待本轮结束加载。
- 14位接收人的实时姓名/open_id和p2p会话全部匹配。
- 已建立18:40下一轮实际投递核验（Codex heartbeat `automation-4`），核验14:00批次和应收下午茶的twice_daily用户；恢复原幂等请求，不给once_daily用户加发。
- 尚待：14位本轮发送/API回读、客户端实际点击及隔离时钟回放。Mac当前锁屏，客户端不能核验；此记录不代表补发已完成。

## 追加的读者合同（2026-09-12）

- 所有外显标题/简介转换为简体中文，原始证据保持原文；AI简介硬性100字符上限，超长重写一次，仍失败即换稿。缓存、独立复核和发送前都检查，不截断文本凑数。
- 首次订阅/修改卡片/成功回执/后台编辑/恢复默认/个人选稿统一保存香港本地或国际优先。旧请求省略字段时保留已选值；显式选择覆盖原宏观例外。
- 20条仅收到6条并非用户设置被改；600秒准备预算错误地结束了整轮。已改为按原轮次累计实际回执；先发送已合格稿，持久化补充队列继续尚欠条数，重启/并发不会重复补发。候选完成限定尝试仍不足时显示真实缺额，不发空卡，不用超时冒充候选耗尽；暂时限流/共享锁超时保留恢复资格。
- 当前聚焦回归214项及43个子测试通过；新增20条切片为6+10+4、重启及三线程恢复不重复、100/101字、超长两次即失败、繁简转换保留英文和原证据、地域保存和排序。后续50项关键回归复测通过。
- Chrome无头浏览器实际运行本项目HTTP处理器及隔离SQLite；1440x1000与390x844均完成地域选择、保存、刷新和API读回，20条设置保持。浏览器插件不可用，使用仓库允许的Playwright。原订阅后台字符串快照测试另有6项既存页面文案/版本差异，未据此宣称全套绿色。
- 实发进度：改动前13/14张原轮卡片已逐项API核验，共54条新闻；剩余Red暂时保留原队列以应用新100字规则，激活后恢复原09:00时间。已发历史卡片不修改，后续补充按新规则。


## 用户停止本轮补发后的最终修复

- 用户明确要求“不要再补充，解决问题确保下次正常”。2026-09-12 10:57:03 已将本轮13笔待发任务移至不可到达的发送时间作为即时保护，并停止本任务的人工补跑进程；当时没有 sending/sent 未确认请求。该保护不修改任何订阅时间或20条设置。正式新代码加载后，调用 `stop_round` 将该轮永久标为 stopped/cancelled，保留真实回执和缺额，重启或补选不得恢复。本轮不得再次发送。
- 廖望本轮为原6条加补1条，即7/20。部分 prepared 不等于条数达标。新增“发送前继续准备”分支：07:00仅准备6条时，07:10继续准备到20条，08:00发送10+10两张卡；发送线程不调用模型。剩余准备窗口约束每层任务，不因补选扩张到约定时间之后。暂时失败不缩减已有有效准备检查点。
- 发现去重错误的真实类型在记账时丢失，暂时排队/网络故障被泛化成“审核失败”。现在原样保留错误类型及原因；AIQueueBusy、timed out、限流和共享锁超时都保留候选资格，后移重试，不把它们记成新闻不合格。优先处理已审核短简介和未尝试候选，现成缓存只影响顺序，不替代任何事实、图片或去重验证。
- 原子 stop 控制同时覆盖直接人工入口、后台恢复、发送前及分卡之间；只阻断明确停止的 content_ref，下一轮维持原兴趣、地区、条数、频率和时间。后台显示“本轮7/20条 · 已停止补发”。
- 当前239项聚焦回归全部通过，覆盖前述真实调用路径以及下一轮仍可入队。旧测试“重启把七个兴趣永久改成四个”与现行保留用户兴趣合同冲突，已改为验证七个保存值和恢复默认均不变；每张卡抽取四板块的现有测试保持。
- 桌面1440x1000、手机390x844继续通过真实HTTP处理器与隔离数据库保存/刷新/API读回，已查看带7/20停止状态的截图。正式客户端仍不可读（CUA cgWindowNotFound）；不把隔离UI或API回读当成原生飞书点击验收。
- Skill 合并全部要求，当前内容哈希 `91323fe32abd108d5a8fa0753cc23487714545daacc2671fb88c92a0c874b925`，quick_validate通过。简体、≤100字符、标题外事实、失败换稿、V4 Pro优先、地区偏好及停止本轮规则均明确。
- 运行逻辑提交 `cb3fb2762`，公共与私有开发分支、私有main快照 `16dfee99900e412aec4f4569dccb301c58c2c06b` 已同步。安全加载请求 `20260912T110532-37193-20730` 等待正在运行的四库资料研究任务结束；不绕过安全加载或中断另一任务。此前10:52:58已加载地域/100字/简体/数量累计和重试轮转版本。
- 18:40 heartbeat `automation-4` 已更新为只核验14:00新轮次及18:30下午茶，明确禁止恢复03:00已停止轮次；下一轮实际回执仍待自然运行核验，不能提前宣称20条实发成功。
