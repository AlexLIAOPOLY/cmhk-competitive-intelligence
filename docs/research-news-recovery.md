# Research and news recovery

2026-09-11

News acceptance review keeps the initial candidate decisions and a durable, unpublished repair draft. A validation error selects the implicated event groups for repair. Other groups remain unchanged. A complete result must still pass the original evidence, field, partition and duplicate checks before any review cell is written. Distinct products, contracts and investment announcements at one conference are separate events.

One input permits at most 12 repair requests and 3 requests for any one scope or candidate, including requests interrupted by a process or network failure. Protocol 6 collects all invalid groups and sends their actual draft and individual errors together. The model returns a single representative ID or null for each event and review field; the program expands equivalent row states and then applies the unchanged evidence validators. This removes contradictory model outputs such as both accepting a row and marking it as a duplicate. Model choices and transport hashes remain in the checkpoint audit. Exhaustion raises `NewsSelectionQualityBlocked`. The scan archive, parent task and generic queue receive the same terminal state and an empty next retry time. Recovery never replays the scan or its notifications. A failed completion receipt retries only the receipt, without another model call.

Four-library recovery uses the original research run and task. Temporary metric failures, including incomplete model/tool output, reopen only those metrics. Existing accepted facts survive the merge and pass formal-table preflight again. Unit handling accepts explicit native currency, scale, count and fiscal-period notation; ambiguous currency and non-exact amounts remain rejected. ARPU values are bound to their specific business label, organic revenue retains its definition, and a total customer chart cannot supply a postpaid count. Candidate recovery and formal replay share all three writer locks continuously.

Zero-selection review uses batches of at most 30 candidates. Each batch has one normal request and at most two repairs; the whole stage permits at most 12 extra repair requests. Normal batch requests are counted separately, so larger valid inputs do not exhaust a retry budget merely by progressing. Request reservations and successful payloads are saved atomically before proceeding. Restarting after a completed batch never calls the model for that batch again, even if the secondary payload cache was not written. Invalid drafts retain all field errors and transport evidence. A source-backed terminal sentence mark is equivalent for quote matching; interior wording and numbers must still match the candidate exactly.

Diagnostic revisions 2 and 3 retain one lifetime migration for an eligible old blocked protocol-6 draft. This preserves its candidate counters and complete attempt history, gives the previously misdiagnosed scope one additional request, and records that migration before calling the model. No later diagnostic revision can grant it again. Revision 3 allowed the previously unmigrated final 32-candidate draft to use the corrected complete weekly schema and streaming transport; the already migrated 65-candidate draft remained byte-for-byte unchanged.

The zero-selection prompt includes one complete JSON example with independent APP and weekly reasons and evidence. Quote matching also recognizes whitespace inserted between Chinese characters during crawling, while preserving the model quote and mapping it to an exact original source span. English word boundaries, numbers and interior punctuation remain strict. A saved draft that passes the corrected full validator can be recovered with zero model requests; its request counts and attempt history remain unchanged and a separate validation audit records the recovery.

The daily dispatcher records up to 6 delayed recoveries, honors user cancellation and detects a dead worker. Successful publication is not whole-run completion while retryable metrics remain. Tool-output recovery retains newly consulted passages and their provenance, including when the initial excerpts had no matching metric. A bounded unsuccessful submission keeps its last validation reason for review. Temporary model failures can use explicitly configured model routes; shared key cooldown and the global rate limiter still apply. Macro indicators refresh independently in incremental research, once per run while the saved database hash matches. AI checkpoints are tied to exact evidence and the analysis protocol and are validated before reuse; they never substitute rule-generated text for AI. A failed focus no longer prevents other focuses and domains from completing and checkpointing, but incomplete analysis still blocks final publication.

The executive read model verifies formal research rows against the original run and storage receipt. Annual updates can replace matching annual cards; quarterly and half-year facts retain their periods as separate model evidence. A missing new-year exchange rate keeps the native amount and its limitation instead of reusing an old rate. Headline and body validation are shared by individual generation and the full publication path.

Complete unsuccessful AI drafts are kept separately from published results. After the normal configured routes fail, a focus permits at most three HTTP requests to revise its explicitly invalid existing fields, including any earlier revision already used. The model receives the actual failed revision, precise new errors, character limits, permitted numeric tokens and the same source evidence. A restart resumes this field correction instead of replaying the complete model routes. Repeated patches or repeated errors without improvement stop early. Unknown paths, entity changes and incomplete repairs are rejected. Every request is reserved before inference and retains original and resulting drafts, models, hashes and its complete audit. Unchanged patch fields remain exact model text and must pass full validation; they cannot hide an unresolved fact error. Generation never deletes unsupported numeric sentences or source URLs in code to make a draft pass.

Writing targets remain 120 characters for a focus body and 28 for its title; model correction prompts aim below those targets. Publication protection limits are 160 and 36 respectively, with at most two complete body sentences. A small overrun that passes every fact, source, entity and reasoning check preserves the complete AI text and receives server-computed presentation warnings. The warnings are recomputed after factual validation and appear in the model analysis and task log; they are not model-provided evidence. Generation, checkpoint reuse, single-card regeneration and final publication share this policy. Corrected validators may accept an archived real response with zero HTTP requests even after its repair budget is spent; the original attempt history and counters remain unchanged.

Cross-domain discoveries use the same title protection limit and a 110-character body target with a 160-character publication limit. Every complete four-discovery response is saved before parsing or validation, including its actual response identity, model and hash. Exact evidence selects its durable recovery record; a restart can parse a saved response even if the process stopped before saving the parsed candidate. Each configured model route permits one reserved HTTP request for that evidence, and restart never refunds or replays spent routes. There are no hidden key or proxy retries within that request; another configured route can continue. Saved packets are revalidated as a complete set of four with zero HTTP, retaining all prior attempts. Two-domain sources, numeric evidence, distinct pairs, complete sentences and unsupported-cause checks remain mandatory; minor length warnings never waive those checks.

Discovery validation also binds each quoted amount to a specific entity, metric, unit, period and exact source. Different currencies, periods or metrics cannot support an affirmative ranking merely because a later sentence adds a limitation. Customer counts cannot establish ARPU differences, and cross-sectional amounts cannot establish decoupling or an unsupported cause. Complete saved packets use at most two local repair requests for their failed items, preserving successful items and every earlier route and repair reservation. Corrections are real model output; a restart can recover a complete saved repair response before consuming another request.

Discovery repair indices accept integers and their exact canonical strings "0" through "3". Booleans, floats, padded or signed strings and out-of-range indices remain invalid. Parsing a saved response with a canonical string does not change its raw model packet or consume another request, including when both repair reservations have already been spent.

Annual sums prefer a verified component's explicitly identified annual disclosure over the first half-year URL. Values, periods and every component source remain intact. Proven source corrections carry their old URL and full composition; existing AI prose is revalidated with a recorded canonical source mapping. Exact previous evidence hashes permit cache migration without resetting model request histories. This prevents a link correction from regenerating already valid analyses and leaves the originally submitted model URLs available for audit.

The inference paths use streamed responses from the existing internal service. This follows a reproduced incident where changed non-streaming requests, model routes and credentials received the identical old HTTP response, while a streamed request returned a new, valid model answer. Executive SSE parsing requires one response identity, a successful end and a complete terminal marker; interrupted content is never published or combined across requests. The transport change addresses the observed workflow failure without claiming that the upstream cache itself has been repaired. Numeric validation preserves decimal precision and recognizes legal thousands separators. Causal negation applies only within its own clause, so an unrelated negative phrase cannot exempt a later unsupported causal claim.

Research and final-review tool calls use the SDK's streaming path with independent request IDs. Raw chunks must retain their response identity and contain complete JSON tool arguments before any tool call is released. Partial streams cannot be replayed through another model; route recovery before the first chunk retains shared limits and key cooldown. A length stop exposes no executable partial tool and preserves the harness's existing token-budget escalation. Actual response ID, model, finish reason and content hash are recorded in research traces. A real fixed arithmetic tool probe verified this path without executing a research tool or writing data.

Completed news receipts can be reconciled repeatedly without model requests, decision changes or notification sends. The parent update supplies its required progress detail and persists the synchronized flag. Dashboard news rounds and pending-round labels use the shared configured scan times, including minutes; historical archives retain their original time and manual review batches are excluded. The browser render signature includes the configured schedule so a changed time refreshes an already rendered card.

The news flow diagram groups recovery attempts by their stable batch ID before considering candidate counts. A failed attempt often lacks that count; it must not become a separate failed batch after a verified recovery. The current health uses the completed, read-back-verified representative, while all prior attempts and other unfinished batches remain visible.

Use the existing queued Web reload to load source changes. Verify activation, runtime hashes and resident monitors before recovery. The existing `data_curation.repair_research_storage` CLI provides a read-only preview and a backed-up application of archived facts. The daily worker CLI resumes the same run. Do not clear successful decisions, reset retry counters, relax evidence checks or replay group notifications to recover a failed batch.

Focused regression coverage includes process interruption, publication-only resume, model tool argument preservation, cache invalidation, partial news repair, terminal-state reconciliation, unit and period boundaries, and the rendered recovery state.

Public publication checks every generated HTML page's local script and stylesheet references against actual output files before pushing. A missing component stops publication with its page and path; remote URLs are excluded from this local check. The bundled news delivery component initializes correctly with an empty public task snapshot while personal delivery records remain private. Chart column values and units use the same financial scale conversion as the other chart types.
