# 个人战略新闻：图文卡片直达原文

2026-09-11 用户确认：只有推送卡片和新闻原文两层。保留早茶/下午茶原标题与封面、原兴趣板块、标题下约100–180字事实简介、右侧缩略图及两个订阅按钮。移除核心看点、头部日期/精选数副标题、点击说明及中间AI详情页。

## 有效链路

- 技能：`cmhk/skills/cmhk-strategic-news-push/SKILL.md`，简介编辑规范在同目录 references/editorial.md；本机安装入口指向这一份源文件。
- 常驻 `SubscriptionDeliveryWorker` 每秒检查个人队列，从个人 due_at 前60分钟开始准备。晚到批次立即准备；原始日期、去重、频率、偏好和发送时间保留。
- `deliver_news(..., prepare_only=True)` 依次选稿、跨发送途径事件去重、解析原文和获取配图、加载技能生成简介、原子保存卡片及审计。发送线程只读准备结果。
- 图片来自原报道或可核实的对应事件来源；无合适图片时省略缩略图，不以订阅封面填补。聚合地址解析未完成不能当原文发送，图片上传失败保留批次重试。
- 公网原图下载兼容本机代理的虚拟DNS：遇198.18.0.0/15时核验真实A和AAAA记录，内网地址仍拒绝。接口依据为[Google Public DNS JSON API](https://developers.google.com/speed/public-dns/docs/doh/json)。CLI上传路径相对运行目录；失败元数据允许下一次准备恢复。
- 每卡最多10条，15/20条订阅分卡且不删简介。超长原文URL导致超限时继续按完整新闻分卡。每张回执单独保存，已成功分卡不重发。
- 技能、模板、应用/封面配置、个人偏好或历史变化使未发准备失效；发送结果不明或已发消息保留原卡、幂等键和外部回执。
- `_send_interactive_card` 同时登记用户/会话/应用，按钮走现有偏好修改和退订确认流程。

## 验证

- 170项相关隔离测试通过：时钟边界、迟到材料、重启、偏好变化、一次每日跳过下午、API失败、分卡失败恢复、读回失败不重发、原文/图片、技能实际输入、原兴趣、底部按钮、代理虚拟DNS及失败素材恢复。
- 实际飞书客户端：本人10条卡片已更新为直达原文。实点PCCW标题和缩略图分别打开HKT官方同一新闻URL，没有中间文档。
- 真实今日10条完成两轮模型准备，第二轮50.22秒；追加图片修复后完整准备49.10秒。真实Google聚合地址成功解析星岛原文、提取原图并上传，10条素材链全量下载上传成功。验收在 `var/subscriptions/direct-source-qa-20260911/`，只向本人发送测试；自动发送仅面向启用订阅者。
- 原生飞书实点两个底部按钮，分别显示本人预填偏好表和取消确认表；未提交修改或退订。测试消息回读、10原文地址和独立图片回读均通过。
- 首次安全激活16:49:50完成，常驻心跳确认60分钟、每秒检查、无准备错误。今晚18:30队列尚未到17:30准备窗口时保持未发送；这不替代自然到点发送验收。图片补丁使用同一安全队列继续加载，最终回执保存在上述目录。
